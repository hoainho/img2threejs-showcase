# img2threejs-showcase

[![Deploy to GitHub Pages](https://github.com/hoainho/img2threejs-showcase/actions/workflows/deploy.yml/badge.svg)](https://github.com/hoainho/img2threejs-showcase/actions/workflows/deploy.yml)
[![PR safety check](https://github.com/hoainho/img2threejs-showcase/actions/workflows/pr-safety-check.yml/badge.svg)](https://github.com/hoainho/img2threejs-showcase/actions/workflows/pr-safety-check.yml)

A Vite + vanilla TypeScript + Three.js gallery of procedural demos generated by
[img2threejs](https://github.com/hoainho/img2threejs) — a tool that rebuilds an
object or character photo as a code-only, animation-ready Three.js model.

**Live gallery:** https://hoainho.github.io/img2threejs-showcase/
Home page lists every demo; `#/demo/:id` opens a full-viewport viewer with
OrbitControls, per-demo studio lighting, and a reference-image side panel.

## Showcases

| Demo | Author | Type | Status |
|---|---|---|---|
| [Sony WF-1000XM3 Earbuds + Case](https://hoainho.github.io/img2threejs-showcase/#/demo/sony-wf1000xm3) | [Hoài Nhớ](https://github.com/hoainho) | object | final |
| [ISSACA 12 Gauge Shotgun](https://hoainho.github.io/img2threejs-showcase/#/demo/issaca-shotgun) | [Hoài Nhớ](https://github.com/hoainho) | object | final |
| [Gerber Paracord Knife](https://hoainho.github.io/img2threejs-showcase/#/demo/gerber-knife) | [Hoài Nhớ](https://github.com/hoainho) | object | final |
| [Doraemon House (isometric diorama)](https://hoainho.github.io/img2threejs-showcase/#/demo/doraemon-house) | [Hoài Nhớ](https://github.com/hoainho) | object | final |
| [War-Hauler "SECTOR 07"](https://hoainho.github.io/img2threejs-showcase/#/demo/warhauler) | [Hoài Nhớ](https://github.com/hoainho) | object | final |
| [Crowned Loot Chest](https://hoainho.github.io/img2threejs-showcase/#/demo/crown-chest) | [Hoài Nhớ](https://github.com/hoainho) | object | placeholder |

This table is maintained by hand — when you add a demo (see below), add a row here too.

## Add your own showcase

1. Generate a model with the [img2threejs](https://github.com/hoainho/img2threejs) skill —
   reconstruction must be code-only, not a downloaded mesh or art pack.
2. Fork this repo, branch off `main`, and add three things:
   - `src/demos/<id>/createXModel.ts` — your factory, as generated
   - a new entry in the `demos` array in `src/demos/registry.ts` (every `DemoEntry`
     field is required — set `status: 'final'`, and `author`/`authorUrl` so you get
     credited on the card and demo page)
   - `public/references/<id>.png` (or `.jpg`/`.jpeg`/`.webp`, ≤ 800 KB)
3. Verify locally: `npm install && npm run build && npm run preview`, check
   `#/demo/<id>`, and run `node scripts/check-showcase-safety.mjs --base main`
   (the same check CI runs).
4. Push your branch and **open a pull request against `main`** — either follow
   the [compare link](https://github.com/hoainho/img2threejs-showcase/compare)
   or run `gh pr create --fill` (the description auto-fills from
   [`PULL_REQUEST_TEMPLATE.md`](.github/PULL_REQUEST_TEMPLATE.md)). Add your
   entry to the table above in the same PR.
5. `pr-safety-check` CI runs automatically and must pass before a maintainer
   reviews. On merge, `deploy.yml` republishes the live gallery automatically.

Full step-by-step, the approval checklist, and what gets a submission rejected
live in **[CONTRIBUTING.md](CONTRIBUTING.md)** — read it before opening a PR.
You can also open the [**Submit a showcase demo**](https://github.com/hoainho/img2threejs-showcase/issues/new/choose)
issue form first to get early feedback on scope before doing the work.

## Stack

- three ^0.169.0, TypeScript ^5.6.0, Vite ^5.4.0
- Hash router only (`#/`, `#/demo/:id`) so GitHub Pages project-site refreshes
  never 404
- No runtime network calls or external CDN/fonts — everything is bundled

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build   # tsc --noEmit && vite build
npm run preview
```

`vite.config.ts` sets `base: '/img2threejs-showcase/'` to match the GitHub
Pages project-site path.

## Project layout

```
src/
  main.ts             bootstrap: mounts the router, handles route changes
  router.ts            tiny hash router
  scene.ts              reusable Three.js viewer (renderer/camera/controls/lights/dispose)
  pages/home.ts         gallery landing page
  pages/demo.ts         per-demo viewer + info panel
  demos/registry.ts     single source of truth for demo metadata + build fn
  demos/<id>/           ported factory source, one folder per demo
```

## Swapping in a placeholder demo

A few registry entries start as `status: 'placeholder'` built from a generic
procedural factory. To replace one with a freshly generated model (as the
maintainer, not via the contributor PR flow above):

1. Drop the new factory file under `src/demos/<id>/` (keep the exported
   function name — copy it in verbatim from the img2threejs output).
2. Update that demo's entry in `src/demos/registry.ts`:
   - point `build` at the new factory's exported function(s),
   - update `referenceImage` if the reference photo changed,
   - set `status: 'final'`.
3. Add the reference image to `public/references/`.

No other files need to change — the router, viewer, and pages read
everything from the registry.
